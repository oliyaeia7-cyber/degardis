Flask==3.0.3
Pillow==10.4.0

# ---- کتابخانه‌های اختیاری برای فعال کردن فرمت‌های اضافی ----
# اگر این دو خط را از کامنت خارج و نصب کنید، تبدیل AVIF و HEIC/HEIF
# هم به‌صورت کامل در سایت فعال می‌شود (بدون آن‌ها، این دو فرمت فقط
# «شناسایی» می‌شوند، نه «تبدیل»):
# pillow-avif-plugin==1.4.6
# pillow-heif==0.18.0
